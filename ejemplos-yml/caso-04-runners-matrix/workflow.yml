name: Runners y matrix

on:
  workflow_dispatch:

jobs:
  runner-sample:
    runs-on: ubuntu-latest

    steps:
      - name: Identificar el runner
        run: |
          echo "OS: $RUNNER_OS"
          echo "Host: $HOSTNAME"
          echo "Arch: $(uname -m)"
          echo "Nombre del runner: $RUNNER_NAME"
          echo "Versión del runner: $RUNNER_VERSION"
          echo "Directorio de trabajo: $RUNNER_WORKSPACE"