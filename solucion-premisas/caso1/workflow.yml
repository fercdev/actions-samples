name: workflow caso1

on:
  push:
  pull_request:
  workflow_dispatch:


jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      
      - name: Setup NodeJS 20
        uses: actions/setup-node@v7
        with:
          node-version: 20
      
      - name: Name Simulando instalar dependencias
        working-directory: app-node
        run: |
          echo "Simulando instalar dependencias, con npm install"
      
      - name: Run tests
        working-directory: app-node
        run: |
          echo "Simulando ejecutar pruebas, con npm test"
