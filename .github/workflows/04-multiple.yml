name: workflow secuencial

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Ejecutar tests
        run: echo "Ejecutando tests desde Actions"

  build:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Construir proyecto
        run: echo "Construyendo proyecto desde Actions"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Desplegando proyecto
        run: echo "Desplegando proyecto desde Actions"